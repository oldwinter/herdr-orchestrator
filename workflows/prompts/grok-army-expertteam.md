在 `/Users/oldwinter/Code/grok-army-campaign/ExpertTeam-Codex` 做一轮「架构」工作。读 README 与模块入口，找一个具体架构问题。用 `gh issue create --repo oldwinter/ExpertTeam-Codex` 开 issue，标题或正文必须包含「架构」。在该 clone 做对应本地改进。不要 push/PR/merge。把 issue URL 与改动文件追加到 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-army/issues.txt`。复用已有 clone。停止条件：issue 可打开且本地有对应改动。
