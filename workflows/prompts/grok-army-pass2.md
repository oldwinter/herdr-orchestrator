这是 grok-army 第二轮。在 `/Users/oldwinter/Code/grok-army-campaign` 里选一个已经有本战役 issue 的 clone，找一个**不同**的「架构 / 体验 / 设计」缺口。

规则：
- 先读该仓库 README 与入口，以及 `.orchestrator/grok-army/issues.txt`，不要重复已有标题。
- 用 `gh issue create` 开一个新的具体 issue，标题或正文必须包含「架构」「体验」或「设计」之一。
- 在该 clone 做对应本地改进。不要 push/PR/merge。
- 把 issue URL、仓库路径、改动文件追加到 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-army/issues.txt`。
- 优先：`herdr-orchestrator`、`hctl`、`all-cli`、`skills-desktop`、`open-pstack`、`herdrm`、`oh-my-mermaid`、`hyperframes`、`canvas-base-md`、`official-prompt-image-gallery`、`multica`、`better-harness`。
停止条件：新 issue 可打开，且本地有对应改动。
