在 `/Users/oldwinter/Code/grok-army-campaign/oh-my-mermaid` 做一轮「设计」工作。

读界面/文档版式/信息架构，找出一个具体设计问题（层级、命名、视觉层次、文档结构）。用 `gh issue create --repo oldwinter/oh-my-mermaid` 开 issue，标题或正文必须包含「设计」。然后在该 clone 做对应本地改进。不要 push / PR / merge。把 issue URL 与改动文件追加到 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-army/issues.txt`。复用已有 clone。停止条件：issue 可打开且本地有对应改动。
