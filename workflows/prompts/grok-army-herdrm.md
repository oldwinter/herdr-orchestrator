在 `/Users/oldwinter/Code/grok-army-campaign/herdrm` 做一轮「体验」工作。

读 README 与 CLI/UI 入口，找出一个具体使用摩擦（帮助文本、错误信息、空状态、命令发现）。用 `gh issue create --repo oldwinter/herdrm` 开 issue，标题或正文必须包含「体验」。然后在该 clone 做对应本地改进。不要 push / PR / merge。把 issue URL 与改动文件追加到 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-army/issues.txt`。复用已有 clone。停止条件：issue 可打开且本地有对应改动。
