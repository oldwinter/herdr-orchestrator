在 `/Users/oldwinter/Code/grok-army-campaign` 里选一个还没有本战役 issue 的 oldwinter clone，做一轮「架构 / 体验 / 设计」工作。

步骤：
1. 先读该仓库 README 与入口代码，找一个具体、可本地修复的问题。主题必须是架构、体验或设计之一，且标题或正文包含对应中文词。
2. 用已登录 `gh issue create` 开一个真实 issue。不要空 issue，不要重复已有同题。
3. 在该 clone 内做对应本地改进。不要 push、不要开 PR、不要 merge。
4. 把 issue URL、仓库路径、改动文件追加写入 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-army/issues.txt`。

优先还没动过的仓库：`herdrm`、`oh-my-mermaid`、`better-harness`、`multica`、`skills`、`harness-studio`、`hyperframes`、`munder-difflin`、`khazix-skills`、`willemstad-x`、`baoyu-skills`、`dg3`、`general-tasks`。
已存在的 clone 必须复用。停止条件：issue URL 可打开，且本地至少有一个对应文件改动。
