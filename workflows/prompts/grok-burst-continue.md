在 `/Users/oldwinter/Code/grok-army-campaign` 里选一个已存在的 oldwinter clone，做一轮新的「架构 / 体验 / 设计」工作。

规则：
- 先读 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-burst/issues.txt`（若存在），不要重复已有标题。
- 读该仓库 README 与入口，找一个具体可本地修复的问题。
- `gh issue create` 开真实 issue，标题或正文必须包含「架构」「体验」或「设计」之一。
- 在该 clone 做对应本地改进。不要 push / PR / merge。
- 把 issue URL、仓库路径、改动文件追加到 `/Users/oldwinter/Code/herdr-orchestrator/.orchestrator/grok-burst/issues.txt`。
- 复用已有 clone。停止条件：issue 可打开且本地有对应改动。
- 这是 3 小时战役的持续供料任务：做完这一项就停，不要空转。
