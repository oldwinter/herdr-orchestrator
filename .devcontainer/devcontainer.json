{
  "name": "herdr-orchestrator",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "26"
    }
  },
  "containerEnv": {
    "UV_LINK_MODE": "copy"
  },
  "postCreateCommand": "python -m pip install uv==0.12.7 && npm install --global rust-just@1.57.0 && npm install --global npm@12.0.2 && git config --file \"$HOME/.gitconfig\" --add safe.directory \"$(pwd)\" && uv sync --locked",
  "customizations": {
    "vscode": {
      "extensions": [
        "charliermarsh.ruff",
        "ms-python.mypy-type-checker",
        "ms-python.python"
      ]
    }
  },
  "remoteUser": "vscode"
}
